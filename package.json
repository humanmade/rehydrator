{
	"name": "rehydrator",
	"private": true,
	"description": "Pattern-based content transformation for WordPress block migrations",
	"scripts": {
		"wp-env": "wp-env",
		"start": "wp-env start",
		"stop": "wp-env stop",
		"test": "wp-env run tests-cli --env-cwd=wp-content/plugins/rehydrator vendor/bin/phpunit"
	},
	"devDependencies": {
		"@wordpress/env": "^10.0.0"
	}
}
