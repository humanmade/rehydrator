<?xml version="1.0"?>
<ruleset name="HM Block Pattern Transformer">
	<description>PHPCS config for HM Block Pattern Transformer plugin</description>

	<rule ref="HM-Minimum" />

	<!-- Matches the composer.json PHP requirement. PHPCompatibility 9.x errors out if this is unset. -->
	<config name="testVersion" value="8.2-" />

	<file>.</file>

	<exclude-pattern>/vendor/*</exclude-pattern>
	<exclude-pattern>/tests/*</exclude-pattern>
	<exclude-pattern>/node_modules/*</exclude-pattern>

	<arg name="extensions" value="php" />
	<arg name="colors" />
	<arg value="sp" />
</ruleset>
